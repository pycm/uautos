Mocks:
    - если модель завершилась с ошибкой (тут нужно различать критичные и нет ошибки), то не вызывать зависящие от нее модели.
    - перечитать доку по Q. Попробовать переписать Ad generator (работа с асинхронностью и Q)

- Проставить индексы в БД и посчитать прирост в скорости
- Пройтись по package.json
- В контроллерах в структуре папок отталкиваться от функциональности (Auth, Youtube, Base, etc), а не от типа контроллера.
  Тип контроллера должен быть просто отражен в названии: TestPageController (соглашение)
- Переделать req, res, stor, env. Слишком много всего. req, res все же должны быть доступны во всех шагах
- Возможность редиректа из шага validate + [234]

------------------------------------------------------------------------------------------------------------------

- Перейти на 0.10.x
- todos in app.js (cluster improvements)
- server.js | пройтись
- logger пересмотреть еще раз
- todos in init.js
- express-validator + validators in local modules + mongoose-validator | заново
- https://github.com/pkrumins/node-lazy | заново
- errors
